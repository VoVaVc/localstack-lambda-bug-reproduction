# localstack-lambda-bug-reproduction
Bug reproduction for issue: https://github.com/localstack/localstack/issues/9291

To repeat the error:

1. run `docker compose up`
2. in ./backend run `npm run local:dev`
